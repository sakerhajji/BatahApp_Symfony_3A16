<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service Après Vente PDF</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            color: #333;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            margin-top: 20px;
            border: 2px solid #333; /* Add border */
        }
        h1 {
            font-size: 28px;
            color: #333;
            margin-bottom: 20px;
            text-align: center;
        }
        p {
            font-size: 16px;
            line-height: 1.5;
            margin-bottom: 10px;
        }
        strong {
            font-weight: bold;
        }
        .signature {
            margin-top: 40px;
            text-align: right;
        }
        .signature img {
            width: 150px;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>Service Après Vente Details</h1>
    <p><strong>Description:</strong> {{ serviceApresVente.description }}</p>
    <p><strong>Type:</strong> {{ serviceApresVente.type }}</p>
    <p><strong>Date:</strong> {{ serviceApresVente.date ? serviceApresVente.date|date('Y-m-d H:i:s') : '' }}</p>
    <p><strong>Status:</strong> {{ serviceApresVente.status ? 'Yes' : 'No' }}</p>

    <div class="signature">
        <img src="path/to/your/signature.png" alt="Signature">
    </div>
</div>
</body>
</html>
