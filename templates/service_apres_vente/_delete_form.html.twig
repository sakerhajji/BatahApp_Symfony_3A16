<form method="post" action="{{ path('app_service_apres_vente_delete', {'idService': service_apres_vente.idService}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_apres_vente.idService) }}">
    <button class="btn">Delete</button>
</form>
