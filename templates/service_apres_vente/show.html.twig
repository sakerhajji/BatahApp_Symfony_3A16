{% extends 'utilisateur/index.html.twig' %}

{% block title %}ServiceApresVente{% endblock %}

{% block body %}
    <h1>ServiceApresVente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdService</th>
                <td>{{ service_apres_vente.idService }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service_apres_vente.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ service_apres_vente.type }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ service_apres_vente.date ? service_apres_vente.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ service_apres_vente.status ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>idPartenaire</th>
                <td>{{ service_apres_vente.idPartenaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="javascript:history.go(-1)">back to list</a>

{% endblock %}
